# 🔍 בדיקת שינויים בשרת מקומי

## איך להריץ:

### אפשרות 1: הרץ את הסקריפט

```cmd
START_LOCAL_SERVER.bat
```

### אפשרות 2: הרץ ידנית

```cmd
npm run dev
```

## מה לבדוק:

### 1. פתח את הדפדפן

השרת יעלה על:
```
http://localhost:5173
```

או:
```
http://127.0.0.1:5173
```

### 2. בדוק את השינויים:

#### Code Splitting:
1. פתח DevTools (F12)
2. לך ל-Network tab
3. רענן את הדף (F5)
4. בדוק שרק chunks נדרשים נטענים
5. נווט בין דפים שונים
6. בדוק שכל route נטען בנפרד

#### Lazy Loading:
1. פתח DevTools → Network
2. רענן את הדף
3. בדוק שרק `index.js` ו-`react-vendor.js` נטענים בהתחלה
4. נווט לדף אחר (למשל `/create-event`)
5. בדוק ש-chunk חדש נטען (למשל `CreateEvent-xxx.js`)

#### Icons:
1. בדוק שכל ה-icons מופיעים
2. בדוק שאין שגיאות בקונסול
3. בדוק שהכפתורים עובדים

#### Version:
1. בדוק את הגרסה בתחתית הדף
2. אמור להיות: `גרסה 1.0.205`

### 3. בדוק את הקונסול:

פתח DevTools → Console ובדוק:
- ✅ אין שגיאות
- ✅ אין warnings על missing imports
- ✅ Lazy loading עובד

### 4. בדוק את ה-Performance:

1. פתח DevTools → Network
2. רענן את הדף
3. בדוק את הזמנים:
   - Bundle ראשוני: אמור להיות קטן (~175 kB)
   - Chunks נוספים: נטענים רק כשצריך

## מה לחפש:

### ✅ אם הכל עובד:
- אין שגיאות בקונסול
- Icons מופיעים
- Lazy loading עובד
- Chunks נטענים נכון
- הגרסה נכונה (1.0.205)

### ❌ אם יש בעיות:
- שגיאות בקונסול → שלח אותן
- Icons לא מופיעים → בדוק את ה-imports
- Lazy loading לא עובד → בדוק את App.tsx
- Chunks לא נטענים → בדוק את vite.config.ts

## אם הכל עובד מקומית:

אז הבעיה היא ב-Render. בדוק:
1. האם ה-commits הגיעו ל-GitHub?
2. האם Render בונה מה-commit הנכון?
3. האם ה-Build Command נכון ב-Render?

## אם יש בעיות גם מקומית:

שלח לי:
1. מה השגיאות בקונסול
2. מה יש ב-Network tab
3. מה יש ב-Logs של השרת
