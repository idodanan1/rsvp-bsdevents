# 📋 מדריך ליצירת טמפלט מאושר ב-Meta/Facebook Business Manager

## ⚠️ הבעיה הנוכחית:

השגיאה 100 אומרת שיש פרמטר לא תקין בטמפלט. זה קורה כי:
1. הפרמטרים לא בסדר הנכון
2. הטמפלט לא תואם למה שהקוד שולח
3. שם הטמפלט או השפה לא נכונים

## ✅ הפתרון: יצירת טמפלט מאושר

### שלב 1: התחבר ל-Facebook Business Manager

1. לך ל: https://business.facebook.com/
2. התחבר לחשבון שלך
3. בחר את ה-Business Account שלך

### שלב 2: צור טמפלט חדש

1. בתפריט השמאלי, לחץ על **WhatsApp**
2. לחץ על **Message Templates**
3. לחץ על **Create Template**

### שלב 3: מלא את הפרטים הבסיסיים

**קטגוריה:** בחר **UTILITY** (מומלץ להזמנות)

**שם הטמפלט:** 
```
event_invitation
```
(או שם אחר - רק אותיות ומספרים, ללא רווחים)

**שפה:** בחר **Hebrew (he)**

### שלב 4: כתוב את תוכן הטמפלט

**⚠️ חשוב מאוד:** הפרמטרים חייבים להיות בסדר הזה:

```
שלום {{1}}!

אנחנו שמחים להזמין אותך ל{{2}} של {{3}} ו{{4}}!

📅 תאריך: {{5}}
🕐 שעה: {{6}}
📍 מיקום: {{7}}

אנא אשר/י הגעה בקישור הבא:
{{8}}

בברכה,
{{3}} ו{{4}} 💕
```

**הסבר הפרמטרים (בסדר!):**
- `{{1}}` = שם האורח (first_name)
- `{{2}}` = סוג האירוע (event_type) - חתונה, בר מצווה וכו'
- `{{3}}` = שם החתן (groom_name)
- `{{4}}` = שם הכלה (bride_name)
- `{{5}}` = תאריך האירוע (event_date)
- `{{6}}` = שעת האירוע (event_time)
- `{{7}}` = מיקום האירוע (venue)
- `{{8}}` = קישור אישור הגעה (guest_response_link)

**⚠️ חובה:** הפרמטרים חייבים להיות בסדר הזה בדיוק! אם תשנה את הסדר, הקוד לא יעבוד.

### שלב 5: שמור וחכה לאישור

1. לחץ על **Submit**
2. הטמפלט יעבור לאישור
3. האישור יכול לקחת בין כמה שעות לכמה ימים
4. תקבל התראה כשהטמפלט מאושר

### שלב 6: עדכן את הקוד

אחרי שהטמפלט מאושר, עדכן את הקוד:

**אופציה 1: דרך קובץ .env**
צור קובץ `.env` בתיקיית הפרויקט:
```env
VITE_WHATSAPP_TEMPLATE_NAME=event_invitation
VITE_WHATSAPP_TEMPLATE_LANGUAGE=he
```

**אופציה 2: עדכן ישירות בקוד**
עדכן את `src/services/messageService.ts`:
```typescript
// במקום hello_world, השתמש בטמפלט שלך
templateName = 'event_invitation';
templateParams = {
  language: 'he',
  paramsOrder: ['first_name', 'event_type', 'groom_name', 'bride_name', 
               'event_date', 'event_time', 'venue', 'guest_response_link'],
  first_name: guest.firstName,
  event_type: event.eventTypeHebrew,
  // ... וכו'
};
```

## 🔧 מה תיקנתי בקוד:

1. **סדר הפרמטרים** - הקוד עכשיו שולח את הפרמטרים בסדר הנכון
2. **תמיכה ב-paramsOrder** - אפשר להגדיר את סדר הפרמטרים
3. **לוגים** - הקוד מדפיס את הפרמטרים שנשלחים כדי שתוכל לבדוק

## 📝 דוגמה לטמפלט פשוט יותר (אם אתה רוצה):

אם אתה רוצה טמפלט פשוט יותר עם פחות פרמטרים:

```
שלום {{1}}!

אנחנו שמחים להזמין אותך לאירוע שלנו!

פרטים נוספים יישלחו בהמשך.

בברכה,
{{2}} ו{{3}}
```

**פרמטרים:**
- `{{1}}` = שם האורח
- `{{2}}` = שם החתן
- `{{3}}` = שם הכלה

## ⚠️ טיפים חשובים:

1. **שם הטמפלט** - חייב להיות ייחודי ולא יכול להיות זהה לטמפלט קיים
2. **שפה** - אם אתה שולח בעברית, בחר Hebrew (he)
3. **קטגוריה UTILITY** - בדרך כלל מאושרת מהר יותר מ-MARKETING
4. **אישור** - יכול לקחת זמן, אז תכנן מראש
5. **סדר הפרמטרים** - חייב להיות זהה לסדר בטמפלט!

## 🐛 פתרון בעיות:

**שגיאה 100:** פרמטר לא תקין
- ✅ ודא שהפרמטרים בסדר הנכון
- ✅ ודא שהשם והשפה של הטמפלט נכונים
- ✅ ודא שהטמפלט מאושר

**שגיאה 132000:** הטמפלט לא קיים
- ✅ ודא שהטמפלט מאושר
- ✅ ודא שהשם והשפה נכונים

**שגיאה 131047:** צריך טמפלט להודעה ראשונה
- ✅ ודא שאתה שולח עם טמפלט מאושר

## 📞 אחרי שהטמפלט מאושר:

1. עדכן את הקוד עם שם הטמפלט החדש
2. בדוק שהפרמטרים תואמים לסדר בטמפלט
3. נסה לשלוח הודעה בדיקה
4. אם הכל עובד, תוכל לשלוח ל-400 אנשים!

