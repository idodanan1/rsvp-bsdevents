# 📸 מדריך להעלאת תמונות למערכת

## ✅ איך להעלות תמונה בצורה נכונה

### שיטה 1: העלאת קובץ תמונה (מומלץ)

1. **לך לדף Dashboard** או **ערוך אירוע**
2. **בחר "העלה קובץ תמונה"** או לחץ על שדה הקובץ
3. **בחר תמונה מהמחשב** (JPG, PNG, GIF, WebP - עד 5MB)
4. **התמונה תועלה אוטומטית** לשרת ותקבל URL

### שיטה 2: הכנסת URL ידנית

1. **העלה את התמונה לשירות ענן** (Google Drive, Dropbox, Imgur, וכו')
2. **קבל את ה-URL הציבורי** של התמונה
3. **הדבק את ה-URL** בשדה "קישור לתמונת הזמנה"

---

## ⚠️ דרישות חשובות לתמונות

### ✅ מה עובד:
- **HTTPS URLs** - תמונות מ-Imgur, Google Drive (ציבורי), Dropbox (ציבורי), וכו'
- **תמונות שהועלו דרך המערכת** - המערכת מנסה להעלות ל-Imgur אוטומטית
- **פורמטים נתמכים**: JPG, PNG, GIF, WebP
- **גודל מקסימלי**: 5MB

### ❌ מה לא עובד:
- **Local file paths** - `C:\Users\...` או `file://...`
- **HTTP URLs** - WhatsApp דורש HTTPS
- **IP מקומי** - `http://192.168.x.x/...` לא יעבוד מחוץ לרשת המקומית
- **קבצים פרטיים** - תמונות מ-Google Drive/Dropbox פרטיים

---

## 🔧 איך המערכת מטפלת בתמונות

### תהליך העלאה:

1. **העלאת קובץ** → המערכת שולחת את הקובץ לשרת
2. **ניסיון העלאה ל-Imgur** → המערכת מנסה להעלות ל-Imgur (HTTPS)
3. **אם Imgur נכשל** → המערכת שומרת את התמונה בשרת המקומי
4. **קבלת URL** → המערכת מחזירה URL לתמונה

### ⚠️ בעיות אפשריות:

#### בעיה: "Image URL is not a valid HTTPS URL"
**פתרון:**
- העלה את התמונה לשירות ענן (Imgur, Google Drive ציבורי)
- או השתמש בפונקציית העלאה של המערכת (היא מנסה להעלות ל-Imgur)

#### בעיה: "Template Header Image Mismatch (Error Code 132012)"
**פתרון:**
- ודא שהתבנית ב-Meta Business Manager מוגדרת עם header image
- או הסר את ה-header image מהתבנית ב-Meta
- ודא שהתמונה היא HTTPS URL תקין

#### בעיה: התמונה לא נשלחת עם ההודעה
**פתרון:**
- ודא שהתמונה היא HTTPS URL תקין
- ודא שהתבנית ב-Meta מוגדרת עם header image component
- בדוק את ה-console logs לפרטים נוספים

---

## 📋 צעדים מומלצים להעלאת תמונה

### שלב 1: הכנת התמונה
- **גודל מומלץ**: עד 5MB
- **פורמט**: JPG או PNG
- **רזולוציה**: עד 1920x1080 (לא חובה, אבל מומלץ)

### שלב 2: העלאת התמונה
1. לך ל-**Dashboard** → לחץ על **"ערוך"** ליד האירוע
2. בחר **"העלה קובץ תמונה"**
3. בחר את הקובץ מהמחשב
4. המתן להעלאה (המערכת תציג הודעה)

### שלב 3: בדיקה
- ודא שה-URL שהתקבל מתחיל ב-`https://`
- אם ה-URL מתחיל ב-`http://`, זה לא יעבוד עם WhatsApp
- בדוק שהתמונה נטענת כראוי בדפדפן

### שלב 4: שימוש בתמונה
- התמונה תישלח אוטומטית עם כל ההודעות
- התמונה תוצג כ-header image בתבנית WhatsApp
- התמונה תוצג גם בדף האירוע

---

## 🆘 פתרון בעיות

### אם התמונה לא נשלחת:

1. **בדוק את ה-URL:**
   ```bash
   # פתח את ה-URL בדפדפן
   # אם התמונה לא נטענת, ה-URL לא תקין
   ```

2. **בדוק את ה-console logs:**
   - פתח את Developer Tools (F12)
   - חפש הודעות עם 🖼️
   - בדוק אם יש שגיאות

3. **נסה להעלות מחדש:**
   - מחק את ה-URL הקיים
   - העלה את התמונה מחדש
   - ודא שההעלאה הצליחה

4. **אם עדיין לא עובד:**
   - העלה את התמונה ידנית ל-Imgur
   - קבל את ה-URL הציבורי
   - הדבק את ה-URL בשדה "קישור לתמונת הזמנה"

---

## 💡 טיפים

1. **השתמש בפונקציית העלאה של המערכת** - היא מנסה להעלות ל-Imgur אוטומטית
2. **ודא שהתמונה היא HTTPS** - זה חובה ל-WhatsApp Business API
3. **בדוק את התמונה לפני שליחה** - פתח את ה-URL בדפדפן
4. **שמור עותק של התמונה** - למקרה שתצטרך להעלות מחדש
5. **השתמש בתמונות קטנות** - זה יעזור לשליחה מהירה יותר

---

## 📞 תמיכה

אם יש לך בעיות עם העלאת תמונות:
1. בדוק את ה-console logs
2. ודא שהשרת רץ (`http://localhost:3002`)
3. בדוק שה-URL הוא HTTPS תקין
4. נסה להעלות תמונה קטנה יותר (פחות מ-5MB)
