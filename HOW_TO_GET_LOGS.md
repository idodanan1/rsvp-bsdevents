# 📋 איך לראות את ה-Logs ב-Render

## למה אני צריך את ה-Logs?

ה-Logs יראו לי את השגיאה המדויקת שגורמת ל-build להיכשל. בלי ה-Logs, אני לא יכול לדעת מה הבעיה.

---

## איך לראות את ה-Logs:

### שלב 1: היכנס ל-Render Dashboard
1. פתח: https://dashboard.render.com/
2. היכנס לחשבון שלך

### שלב 2: מצא את השירות
1. לחץ על **"Blueprints"** בתפריט השמאלי
2. לחץ על **"אישורי הגעה"** (או שם הבלופרינט שלך)
3. לחץ על **"Resources"** בתפריט השמאלי
4. לחץ על **"rsvp-frontend"**

### שלב 3: פתח את ה-Logs
1. לחץ על **"Logs"** בתפריט העליון
2. תראה את כל ה-Logs של ה-build

### שלב 4: העתק את ה-Logs
1. גלול למטה עד שתמצא את ה-Logs של ה-build שנכשל
2. העתק את כל ה-Logs (Ctrl+A, Ctrl+C)
3. שלח לי את ה-Logs

---

## מה לחפש ב-Logs:

חפש שורות שמתחילות ב:
- ❌ **Error:**
- ❌ **Failed:**
- ❌ **Cannot find:**
- ❌ **Module not found:**
- ❌ **Type error:**

אלה יראו לי את השגיאה המדויקת.

---

## דוגמה למה שאני צריך:

```
Error: Cannot find module 'xyz'
Failed to resolve import 'abc'
Type error: Property 'xyz' does not exist
```

---

## 💡 טיפ:

אם ה-Logs ארוכים מדי, העתק רק את החלק האחרון (השורות האחרונות) - שם בדרך כלל נמצאות השגיאות החשובות.

---

**שלח לי את ה-Logs ואני אתקן את הבעיה מיד! 🚀**

