# 🔍 דיבוג בעיית התחברות ממחשב אחר

## מה לבדוק:

### 1. פתח את הקונסול במחשב השני

1. **פתח את הדף:** https://rsvp-frontend-wy47.onrender.com/login
2. **לחץ F12** כדי לפתוח את הקונסול
3. **נסה להתחבר**
4. **שלח לי את כל השגיאות** שאתה רואה בקונסול

### 2. בדוק את ה-Backend

האם ה-backend רץ? בדוק ב:
- https://whatsapp-backend-enfz.onrender.com/api/health

אם זה לא עובד, ה-backend לא רץ.

### 3. בדוק את ה-URL

בקונסול, חפש את ההודעה:
```
🔐 Login attempt: { email: "...", backendUrl: "..." }
```

**שלח לי מה כתוב שם.**

### 4. בדוק שגיאות רשת

בקונסול, לחץ על **Network** (רשת) ולנסה להתחבר שוב.

חפש בקשות ל-`/api/users/login`:
- **אם יש שגיאה 404** → ה-backend לא רץ או ה-URL לא נכון
- **אם יש שגיאה CORS** → יש בעיה ב-CORS
- **אם יש שגיאה 500** → יש שגיאה ב-backend

**שלח לי את כל הפרטים!**

