# 📸 איך להעלות תמונת הזמנה

## ⚠️ הבעיה:
התמונה שלך היא קובץ מקומי (`file:///C:/Users/...`) ולא ניתן לשלוח אותה דרך WhatsApp Business API.

## ✅ הפתרון:

### שלב 1: לך ל-Dashboard
1. פתח את המערכת בדפדפן
2. לך ל-Dashboard (הדף הראשי)

### שלב 2: ערוך את האירוע
1. מצא את האירוע שלך ברשימה
2. לחץ על כפתור "ערוך" (Edit) ליד האירוע

### שלב 3: העלה את התמונה
1. במסך העריכה, מצא את השדה "תמונת הזמנה"
2. לחץ על "בחר קובץ" או "Upload Image"
3. בחר את התמונה מהמחשב שלך
4. התמונה תועלה אוטומטית לשרת
5. לחץ "שמור" (Save)

### שלב 4: בדוק שהתמונה הועלתה
- אחרי ההעלאה, תראה URL של התמונה (למשל: `http://localhost:3002/uploads/image-123456.jpg`)
- זה אומר שהתמונה הועלתה בהצלחה

### שלב 5: שלח הודעה
- עכשיו כשתשלח הודעה, התמונה תישלח אוטומטית!

## 🔍 איך לבדוק:
1. פתח את הקונסול בדפדפן (F12)
2. שלח הודעה
3. חפש את ההודעה: `🖼️ Image URL: http://localhost:3002/uploads/...`
4. אם אתה רואה URL כזה (ולא `file://`), זה אומר שהתמונה תקינה!

## ❓ בעיות נפוצות:

### "התמונה לא נשלחת"
- ודא שהשרת רץ: `cd whatsapp-backend && node server.js`
- ודא שהתמונה היא URL ולא קובץ מקומי

### "התמונה לא מופיעה"
- ודא שהתמונה הועלתה בהצלחה (תראה URL ב-Dashboard)
- ודא שהשרת רץ על פורט 3002

### "אין אפשרות להעלות תמונה"
- ודא שהשרת רץ
- בדוק שהתיקייה `whatsapp-backend/uploads/` קיימת


