# 🔧 תיקון שגיאת Environment Variables - כפילות

## הבעיה:

יש שני משתנים עם אותו KEY `VITE_BACKEND_URL`, וזה גורם לשגיאה:
```
Duplicate keys are not allowed.
```

---

## הפתרון:

### שלב 1: מחק את אחד מהמשתנים הכפולים

1. **לחץ על אייקון הפח (🗑️)** של אחד מהמשתנים
   - זה ימחק את המשתנה הכפול

2. **השאר רק את המשתנה עם הערך:**
   ```
   KEY: VITE_BACKEND_URL
   VALUE: https://whatsapp-backend.onrender.com
   ```

### שלב 2: שמור את השינויים

1. **לחץ על "Save only"** בתחתית הדף
2. Render יבנה מחדש את ה-Frontend עם המשתנה החדש

---

## מה לעשות:

### אפשרות 1: מחק את המשתנה הראשון
- אם המשתנה הראשון (עם ה-masked value) לא נחוץ
- לחץ על אייקון הפח שלו
- שמור

### אפשרות 2: מחק את המשתנה השני
- אם המשתנה השני (עם הערך המלא) לא נחוץ
- לחץ על אייקון הפח שלו
- שמור

**המלצה:** השאר את המשתנה עם הערך המלא (`https://whatsapp-backend.onrender.com`)

---

## אחרי התיקון:

אחרי שתמחק את הכפילות ותשמור:
1. ✅ השגיאה תיעלם
2. ✅ Render יבנה מחדש את ה-Frontend
3. ✅ ה-Frontend יוכל להתחבר ל-Backend

---

## 💡 טיפים:

- **תמיד בדוק שאין כפילות** לפני שמירה
- **השתמש בערך המלא** של ה-URL
- **אם יש ספק** - מחק את שני המשתנים והוסף אחד חדש

---

**עכשיו מחק את הכפילות ושמור! 🚀**

