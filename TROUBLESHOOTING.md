# 🔧 פתרון בעיות - הדף לא נטען

## ✅ בדיקות ראשוניות

### 1. ודא שהשרת רץ
1. פתח את **Terminal/PowerShell**
2. לך לתיקיית הפרויקט
3. הפעל: `npm run dev` או `go.bat`
4. ודא שאתה רואה:
   ```
   VITE v4.x.x  ready in xxx ms
   ➜  Local:   http://localhost:5173/
   ```

### 2. בדוק את הקונסול בדפדפן
1. לחץ **F12** או **Ctrl+Shift+I** לפתיחת Developer Tools
2. לך לטאב **Console**
3. חפש שגיאות (אדומות)
4. העתק את השגיאות ושלח אותן

### 3. בדוק את הטאב Network
1. ב-Developer Tools, לך לטאב **Network**
2. רענן את הדף (F5)
3. בדוק אם יש בקשות שנכשלו (אדומות)
4. לחץ על בקשה שנכשלה כדי לראות פרטים

## 🔍 בעיות נפוצות ופתרונות

### בעיה: "Cannot GET /" או דף ריק
**פתרון:**
1. ודא שהשרת רץ על פורט 5173
2. נסה לפתוח: `http://localhost:5173/`
3. אם זה לא עובד, נסה: `http://127.0.0.1:5173/`

### בעיה: "Failed to fetch" או שגיאות רשת
**פתרון:**
1. ודא שאין Firewall חוסם
2. בדוק שהפורט 5173 לא תפוס
3. נסה להפעיל מחדש את השרת

### בעיה: שגיאת JavaScript בקונסול
**פתרון:**
1. פתח את הקונסול (F12)
2. העתק את השגיאה המלאה
3. בדוק אם השגיאה מתייחסת לקובץ ספציפי
4. אם השגיאה היא ב-`whatsappService.ts` או `smsService.ts`, זה תקין - אלה רק אזהרות

### בעיה: "Module not found" או שגיאות import
**פתרון:**
1. סגור את השרת (Ctrl+C)
2. מחק את תיקיית `node_modules`
3. הפעל: `npm install`
4. הפעל מחדש: `npm run dev`

### בעיה: הדף נטען אבל ריק/לבן
**פתרון:**
1. פתח את הקונסול (F12)
2. בדוק אם יש שגיאות JavaScript
3. בדוק את הטאב **Elements** - האם יש `<div id="root">`?
4. אם אין, יש בעיה ב-`index.html`

## 🚀 הפעלה מחדש מלאה

אם שום דבר לא עובד, נסה הפעלה מחדש מלאה:

1. **סגור את השרת** (Ctrl+C)
2. **סגור את הדפדפן** לחלוטין
3. **מחק את ה-cache:**
   ```powershell
   Remove-Item -Recurse -Force node_modules
   Remove-Item -Force package-lock.json
   ```
4. **התקן מחדש:**
   ```powershell
   npm install
   ```
5. **הפעל מחדש:**
   ```powershell
   npm run dev
   ```
6. **פתח דפדפן חדש** ב-`http://localhost:5173/`

## 📞 אם עדיין לא עובד

אם שום דבר לא עוזר, שלח:
1. **השגיאה המלאה מהקונסול** (F12 → Console)
2. **הפלט מהטרמינל** (מה שהשרת מציג)
3. **גרסת Node.js:** `node --version`
4. **גרסת npm:** `npm --version`

## 🔍 בדיקות נוספות

### בדוק את גרסת Node.js
```powershell
node --version
```
צריך להיות **v16 או יותר גבוה**

### בדוק את גרסת npm
```powershell
npm --version
```

### בדוק אם הפורט תפוס
```powershell
netstat -ano | findstr :5173
```
אם יש תהליך, סגור אותו או שנה את הפורט ב-`vite.config.ts`

