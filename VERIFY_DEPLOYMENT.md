# ✅ איך לבדוק אם השינויים מופיעים בשרת

## בדיקה מהירה:

### שלב 1: פתח את האפליקציה

**פתח:** https://rsvp-frontend-wy47.onrender.com

### שלב 2: התחבר למערכת

1. לחץ על "התחבר" או גש ל: https://rsvp-frontend-wy47.onrender.com/login
2. הזן:
   - אימייל: `idodanan1@gmail.com`
   - סיסמה: `QPwo1029`
3. לחץ "התחבר"

### שלב 3: בדוק את הדשבורד

**אחרי ההתחברות, בדוק את הדשבורד:**

**מה לחפש:**
- מתחת לכותרת "דשבורד"
- מתחת לטקסט "בס"ד אירועים - אישורי הגעה וסידורי הושבה"
- **אמור להיות:** `✅ מעודכן: [תאריך ושעה]`

**אם אתה רואה את התאריך והשעה** → ✅ הפרויקט מחובר נכון!
**אם אתה לא רואה את התאריך** → ❌ Render מחובר ל-repository אחר

---

## דרך נוספת - בדיקה דרך הקוד:

### שלב 1: פתח את האפליקציה

**פתח:** https://rsvp-frontend-wy47.onrender.com

### שלב 2: פתח את ה-Developer Tools

1. לחץ `F12` או `Ctrl + Shift + I`
2. לחץ על "Sources" או "Debugger"
3. מצא את `Dashboard.tsx`

### שלב 3: בדוק את הקוד

**חפש את השורה:**
```typescript
בס"ד אירועים - אישורי הגעה וסידורי הושבה ✅ מעודכן: {new Date().toLocaleString('he-IL')}
```

**אם אתה רואה את השורה הזו** → ✅ הפרויקט מחובר נכון!
**אם אתה רואה רק:**
```typescript
בס"ד אירועים - אישורי הגעה וסידורי הושבה
```
**בלי התאריך** → ❌ Render מחובר ל-repository אחר

---

## מה לעשות אם זה לא הפרויקט הנכון:

אם אתה לא רואה את התאריך, זה אומר ש-Render מחובר ל-repository אחר.

### פתרון:

1. **ב-Render Dashboard → `rsvp-frontend`**
2. **לחץ על "Settings"** (אם אתה מוצא)
3. **מצא "Repository"**
4. **לחץ על "Change Repository"**
5. **בחר:** `idodanan1/-rsvp-management-system`
6. **שמור**
7. **לחץ "Manual Deploy" → "Deploy latest commit"**
8. **המתן 5-10 דקות**

---

**תאריך:** $(Get-Date)



