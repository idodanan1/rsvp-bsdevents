# 💳 התחלה מהירה עם Stripe

## שלב 1: צור חשבון Stripe (2 דקות)

1. היכנס ל: **https://dashboard.stripe.com/**
2. לחץ **"Sign up"** (הרשמה)
3. מלא פרטים:
   - אימייל
   - סיסמה
   - שם חברה (אופציונלי)

## שלב 2: קבל מפתחות API (1 דקה)

1. אחרי ההתחברות, עבור ל:
   - **Developers** → **API keys**
2. תראה 2 מפתחות:
   - **Publishable key** (מתחיל ב-`pk_test_...`)
   - **Secret key** (מתחיל ב-`sk_test_...`)

## שלב 3: העתק את המפתחות לקבצים (2 דקות)

### 1. Publishable Key (לשימוש ב-Frontend):

פתח את הקובץ: **`.env`** (בתיקיית הראשית)

הוסף:
```env
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51...
```

### 2. Secret Key (לשימוש ב-Backend):

פתח את הקובץ: **`whatsapp-backend/.env`**

הוסף:
```env
STRIPE_SECRET_KEY=sk_test_51...
```

## שלב 4: רענן את השרת

1. עצור את השרת (Ctrl+C)
2. הרץ שוב: **`START_BACKEND.bat`**

## ✅ זהו! Stripe מוכן לשימוש

עכשיו תוכל:
- לראות תשלומים בדשבורד המנהל
- לקבל תשלומים אמיתיים
- לראות היסטוריית תשלומים

---

## 🧪 בדיקה (אופציונלי)

אם אתה רוצה לבדוק שהכל עובד:

1. היכנס לדף **"רכוש רשומות"**
2. בחר חבילה
3. השתמש בכרטיס בדיקה של Stripe:
   - מספר: `4242 4242 4242 4242`
   - תאריך: כל תאריך עתידי (למשל: 12/25)
   - CVC: כל 3 ספרות (למשל: 123)
   - ZIP: כל 5 ספרות (למשל: 12345)

---

## ⚠️ הערות חשובות

- **Test Mode**: ב-Stripe Dashboard, ודא שאתה ב-**Test mode** (לא Live mode) לפיתוח
- **Webhook**: לא חובה לפיתוח מקומי - רק ל-production
- **מחיר**: Stripe לא גובה כסף ב-Test mode

---

## 🆘 בעיות?

אם יש בעיה:
1. ודא שהמפתחות נכונים
2. ודא שהשרת רץ מחדש אחרי הוספת המפתחות
3. בדוק את הקונסול לשגיאות

