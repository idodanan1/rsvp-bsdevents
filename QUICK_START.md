# 🚀 הפעלה מהירה

## ✅ השרת כבר רץ!

השרת פועל על: **http://localhost:5173/**

## 📋 הוראות:

1. **פתח דפדפן** (Chrome, Edge, Firefox)
2. **לך לכתובת:** `http://localhost:5173/`
3. **אם הדף לא נטען:**
   - לחץ **F12** לפתיחת Developer Tools
   - לך לטאב **Console**
   - העתק כל שגיאה (אדומה) ושלח

## 🔧 אם השרת לא רץ:

### אפשרות 1: הפעל עם go.bat
1. לחץ כפול על `go.bat`
2. חכה שהשרת יתחיל
3. פתח דפדפן ב-`http://localhost:5173/`

### אפשרות 2: הפעל ידנית
```powershell
npm run dev
```

### אפשרות 3: הפעל עם Vite ישירות
```powershell
npx vite --host 0.0.0.0 --port 5173
```

## ⚠️ בעיות נפוצות:

### "Cannot GET /"
- ודא שהשרת רץ
- בדוק שהפורט 5173 לא תפוס
- נסה להפעיל מחדש את השרת

### דף ריק/לבן
- פתח את הקונסול (F12)
- בדוק אם יש שגיאות JavaScript
- שלח את השגיאות

### "Port already in use"
- סגור את השרת הקודם (Ctrl+C)
- או שנה את הפורט ב-`vite.config.ts`

## 📞 אם עדיין לא עובד:

1. פתח את הקונסול (F12 → Console)
2. העתק את כל השגיאות
3. שלח את השגיאות + מה שאתה רואה על המסך

