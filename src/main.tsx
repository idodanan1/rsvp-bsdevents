import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

// Render immediately without StrictMode for faster initial load
ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
