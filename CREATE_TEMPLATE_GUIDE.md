# מדריך ליצירת טמפלט מאושר ב-Meta/Facebook Business Manager

## למה צריך טמפלט מאושר?

WhatsApp Business API דורש טמפלט מאושר להודעות ראשונות. הטמפלט `hello_world` הוא רק לבדיקות ולא מתאים להזמנות.

## איך ליצור טמפלט מאושר:

### שלב 1: התחבר ל-Facebook Business Manager

1. לך ל: https://business.facebook.com/
2. התחבר לחשבון שלך
3. בחר את ה-Business Account שלך

### שלב 2: נווט לטמפלטים

1. בתפריט השמאלי, לחץ על **WhatsApp**
2. לחץ על **Message Templates** (תבניות הודעות)
3. לחץ על **Create Template** (צור תבנית)

### שלב 3: בחר קטגוריה

בחר אחת מהקטגוריות:
- **UTILITY** - למידע חשוב (מומלץ להזמנות)
- **MARKETING** - לפרסום ומכירות
- **AUTHENTICATION** - לאימות

להזמנות, מומלץ לבחור **UTILITY**.

### שלב 4: מלא פרטי הטמפלט

**שם הטמפלט:**
```
event_invitation
```
(או שם אחר שתרצה, אבל צריך להיות באנגלית, ללא רווחים, רק אותיות ומספרים)

**שפה:**
בחר **Hebrew (he)** או **English (en)**

**קטגוריה:**
UTILITY (או MARKETING אם בחרת)

### שלב 5: כתוב את תוכן ההודעה

**דוגמה לטמפלט להזמנה:**

```
שלום {{1}}!

אנחנו שמחים להזמין אותך ל{{2}} של {{3}} ו{{4}}!

📅 תאריך: {{5}}
🕐 שעה: {{6}}
📍 מיקום: {{7}}

אנא אשר/י הגעה בקישור הבא:
{{8}}

בברכה,
{{3}} ו{{4}} 💕
```

**הערות חשובות:**
- `{{1}}` = שם האורח
- `{{2}}` = סוג האירוע (חתונה, בר מצווה וכו')
- `{{3}}` = שם החתן
- `{{4}}` = שם הכלה
- `{{5}}` = תאריך האירוע
- `{{6}}` = שעת האירוע
- `{{7}}` = מיקום האירוע
- `{{8}}` = קישור אישור הגעה

**חשוב:**
- כל משתנה צריך להיות `{{מספר}}` (1, 2, 3 וכו')
- לא ניתן להשתמש ב-emoji בטמפלטים (למעט כמה בסיסיים)
- הקישור צריך להיות URL מלא (https://...)

### שלב 6: שמור וחכה לאישור

1. לחץ על **Submit** (שלח)
2. הטמפלט יעבור לאישור
3. האישור יכול לקחת בין כמה שעות לכמה ימים
4. תקבל התראה כשהטמפלט מאושר

### שלב 7: עדכן את הקוד

אחרי שהטמפלט מאושר, עדכן את הקוד:

1. פתח את הקובץ `.env` (או צור אותו אם לא קיים)
2. הוסף:
```env
VITE_WHATSAPP_TEMPLATE_NAME=event_invitation
VITE_WHATSAPP_TEMPLATE_LANGUAGE=he
```

3. או עדכן את הקוד ב-`src/services/messageService.ts`:
```typescript
templateName = 'event_invitation';
templateParams = {
  language: 'he',
  // פרמטרים לפי הסדר בטמפלט
};
```

## דוגמה לטמפלט פשוט יותר (ללא פרמטרים רבים):

אם אתה רוצה טמפלט פשוט יותר:

```
שלום!

אנחנו שמחים להזמין אותך לאירוע שלנו!

פרטים נוספים יישלחו בהמשך.

בברכה,
{{1}} ו{{2}}
```

זה טמפלט פשוט עם רק 2 פרמטרים (שמות הזוג).

## טיפים:

1. **שם הטמפלט** - צריך להיות ייחודי ולא יכול להיות זהה לטמפלט קיים
2. **שפה** - אם אתה שולח בעברית, בחר Hebrew
3. **קטגוריה UTILITY** - בדרך כלל מאושרת מהר יותר מ-MARKETING
4. **אישור** - יכול לקחת זמן, אז תכנן מראש

## מה לעשות אחרי שהטמפלט מאושר:

1. עדכן את הקוד להשתמש בשם הטמפלט החדש
2. בדוק שהפרמטרים תואמים לסדר בטמפלט
3. נסה לשלוח הודעה בדיקה
4. אם הכל עובד, תוכל לשלוח ל-400 אנשים!

## בעיות נפוצות:

**שגיאה 100:** פרמטר לא תקין
- ודא שהפרמטרים תואמים לסדר בטמפלט
- ודא שהשם והשפה של הטמפלט נכונים

**שגיאה 132000:** הטמפלט לא קיים
- ודא שהטמפלט מאושר
- ודא שהשם והשפה נכונים

**שגיאה 131047:** צריך טמפלט להודעה ראשונה
- ודא שאתה שולח עם טמפלט מאושר

